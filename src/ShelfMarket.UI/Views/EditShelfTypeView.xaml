<Page x:Class="ShelfMarket.UI.Views.EditShelfTypeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="clr-namespace:ShelfMarket.UI.Views.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Tilføj / rediger reol typer">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Tilføj og rediger reol typer"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- Form -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                     MaxHeight="20" MinWidth="200" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="3" Text="Description:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBox Grid.Column="4" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                     MaxHeight="20" MinWidth="200" VerticalAlignment="Center"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Tilføj" Width="80" Command="{Binding AddCommand}"
                    Visibility="{Binding IsAddMode, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Gem" Width="80" Command="{Binding SaveCommand}"
                    Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Slet" Width="80" Command="{Binding DeleteCommand}"
                    Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>
            <!--<Button Content="Nulstil" Width="80" Command="{Binding ResetCommand}"
                    Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>-->
            <Button Content="Annuller" Width="80" Command="{Binding CancelCommand}"/>
        </StackPanel>

        <!-- ListView/UserControl -->
        <controls:ListShelfTypeView Grid.Row="3" x:Name="ShelfTypesListControl" Margin="0,10,0,0"/>

        <!-- Error and status messages -->
        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="{Binding Error}" Foreground="Red" FontWeight="Bold" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding InfoMessage}" Foreground="Green" FontWeight="Bold" Visibility="{Binding HasInfoMessage, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>
    </Grid>
</Page>
