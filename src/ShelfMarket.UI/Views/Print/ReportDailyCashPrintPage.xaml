<Page x:Class="ShelfMarket.UI.Views.Print.ReportDailyCashPrintPage"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:system="clr-namespace:System;assembly=System.Runtime"
           xmlns:vm="clr-namespace:ShelfMarket.UI.ViewModels.Reports" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           d:DataContext="{d:DesignInstance Type=vm:ReportDailyCashViewModel}"
           Width="794" Height="1123"
           Background="White">

    <!-- Optional design-time VM -->
    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Border">
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>
    </Page.Resources>

    <Grid DataContext="{Binding}" Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel>
            <TextBlock Text="Kasserapport" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBlock Text="{Binding Date, StringFormat='Dato: {0:yyyy-MM-dd}'}"/>
            <TextBlock Text="{Binding OpeningCash, StringFormat='Åbningsbeholdning: {0:C}', ConverterCulture=da-DK}"/>
        </StackPanel>

        <!-- Summary figures -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,18,0,10">
            <Border>
                <StackPanel>
                    <TextBlock Text="Kontantsalg (system)" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding CashSalesSystem, StringFormat={}{0:C}, ConverterCulture=da-DK}"/>
                </StackPanel>
            </Border>
            <Border>
                <StackPanel>
                    <TextBlock Text="Forventet kontant" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ExpectedCash, StringFormat={}{0:C}, ConverterCulture=da-DK}"/>
                </StackPanel>
            </Border>
            <Border>
                <StackPanel>
                    <TextBlock Text="Optalt kontant" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding CountedCash, StringFormat={}{0:C}, ConverterCulture=da-DK}"/>
                </StackPanel>
            </Border>
            <Border>
                <StackPanel>
                    <TextBlock Text="Difference" FontWeight="SemiBold"/>
                    <TextBlock>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="{Binding Difference, StringFormat={}{0:C}, ConverterCulture=da-DK}"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Difference}" Value="0">
                                        <Setter Property="Foreground" Value="DarkGreen"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Difference}" Value="-0">
                                        <Setter Property="Foreground" Value="DarkGreen"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Difference}">
                                        <DataTrigger.Value>
                                            <system:Decimal>0</system:Decimal>
                                        </DataTrigger.Value>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Body -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Denominations table -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Optælling" FontWeight="SemiBold" Margin="0,0,0,6"/>

                <TextBlock Text="Værdi" Width="70" FontWeight="SemiBold" Margin="0,0,0,0"/>
                <TextBlock Text="Antal" Width="60" FontWeight="SemiBold" Margin="70,0,0,0"/>
                <TextBlock Text="Beløb" Width="80" FontWeight="SemiBold" Margin="130,0,0,4"/>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Denominations}">
                    <ItemsControl.Resources>
                        <Style TargetType="ItemsControl">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ItemsControl">
                                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="0,1,0,1"/>
                        </Style>
                    </ItemsControl.Resources>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel LastChildFill="False">
                                <TextBlock Text="{Binding Value, StringFormat={}{0:C}, ConverterCulture=da-DK}" Width="70"/>
                                <TextBlock Text="{Binding Count}" Width="60"/>
                                <TextBlock Text="{Binding Amount, StringFormat={}{0:C}, ConverterCulture=da-DK}" Width="80"/>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <!-- Note + signatures -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Notat" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <Border>
                    <TextBlock Text="{Binding Note}" TextWrapping="Wrap" MinHeight="100"/>
                </Border>

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Kasserer underskrift" Margin="0,0,0,4"/>
                        <Border Height="70" Background="#FFF"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="20,0,0,0">
                        <TextBlock Text="Dato / Tid" Margin="0,0,0,4"/>
                        <Border Height="70" Background="#FFF"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="20,0,0,0">
                        <TextBlock Text="Kontrol underskrift" Margin="0,0,0,4"/>
                        <Border Height="70" Background="#FFF"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>

        <!-- Footer -->
        <TextBlock Grid.Row="3"
                   Margin="0,30,0,0"
                   FontSize="11"
                   Text="Kasserapport skal opbevares jf. bogføringsloven. Sørg for korrekt optælling og underskrift."/>
    </Grid>
</Page>