<Page x:Class="ShelfMarket.UI.Views.ManagesShelfTanentContractLineView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="clr-namespace:ShelfMarket.UI.Views.UserControls"
      xmlns:viewmodels="clr-namespace:ShelfMarket.UI.ViewModels" 
      d:DataContext="{d:DesignInstance Type=viewmodels:ManagesShelfTanentContractLineViewModel}"
      mc:Ignorable="d"
      Title="Reollejer kontrakt linier">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Administrere reol lejere"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            <DatePicker Grid.Column="0" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" 
                     MaxHeight="25" MinWidth="200" VerticalAlignment="Center"/>
            <DatePicker Grid.Column="3" SelectedDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}" 
                     MaxHeight="25" MinWidth="200" VerticalAlignment="Center"/>
        </Grid>
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Tilføj" Width="80" Command="{Binding AddCommand}"
                    Visibility="{Binding IsAddMode, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Gem" Width="80" Command="{Binding SaveCommand}"
                    Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Slet" Width="80" Command="{Binding DeleteCommand}"
                    Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Annuller" Width="80" Command="{Binding CancelCommand}"/>
        </StackPanel>
        <!-- ListView/UserControl -->
        <controls:ManagesShelfTanentContractLineListView Grid.Row="3" x:Name="ManagesShelfTanentContractLineListViewControl" Margin="0,10,0,0"/>

        <!-- Error and status messages -->
        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="{Binding Error}" Foreground="Red" FontWeight="Bold" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding InfoMessage}" Foreground="Green" FontWeight="Bold" Visibility="{Binding HasInfoMessage, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>
    </Grid>
</Page>
