<UserControl x:Class="ShelfMarket.UI.Views.UserControls.EanLabelGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:components="clr-namespace:ShelfMarket.UI.Views.UserControls.Components"
             xmlns:vm="clr-namespace:ShelfMarket.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:EanLabelGeneratorViewModel}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Form -->
        <StackPanel Orientation="Horizontal">
            <!-- Tenant email search (replaces tenant dropdown) -->
            <StackPanel>
                <TextBlock Text="Lejer email" Margin="0,0,0,4"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="200"
                             Text="{Binding SearchEmail, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Indtast lejerens email"/>
                    <Button Content="Søg"
                            Margin="6,0,0,0"
                            Command="{Binding SearchTenantCommand}"/>
                    <Button Content="Ryd"
                            Margin="6,0,0,0"
                            Command="{Binding ClearTenantCommand}"/>
                </StackPanel>
                <TextBlock Text="{Binding SearchResultMessage}"
                           Margin="0,4,0,0"
                           Foreground="#555"/>
                <TextBlock Text="{Binding TenantDisplayName}"
                           Margin="0,2,0,0"
                           FontWeight="SemiBold"
                           Visibility="{Binding HasTenant, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Vælg dato" Margin="16,0,0,4"/>
                <components:MonthYearPicker
                    SelectedDate="{Binding SelectedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    MaxHeight="25" MinWidth="200"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Reol" Margin="16,0,0,4"/>
                <ComboBox Width="140"
                          ItemsSource="{Binding ShelfNumbers}"
                          SelectedItem="{Binding SelectedShelf, Mode=TwoWay}"
                          IsEditable="False"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Pris" Margin="16,0,0,4"/>
                <TextBox Width="120"
                         HorizontalContentAlignment="Right"
                         Text="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>
            </StackPanel>

            <Button Content="Generer"
                    Margin="16,16,0,0"
                    Padding="14,6"
                    Command="{Binding GenerateCommand}"/>
        </StackPanel>

        <!-- Preview -->
        <Border Grid.Row="1" Margin="0,16,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Reol:" FontWeight="SemiBold" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding ShelfDisplay}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Pris:" FontWeight="SemiBold" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding PriceDisplay}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="EAN:" FontWeight="SemiBold" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding Ean}" FontFamily="Consolas"/>
                    </StackPanel>
                </StackPanel>

                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <Image Source="{Binding BarcodeImage}" Stretch="None" />
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Errors -->
        <TextBlock Grid.Row="2"
                   Margin="0,12,0,0"
                   Foreground="Firebrick"
                   TextWrapping="Wrap"
                   Text="{Binding Error}"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>