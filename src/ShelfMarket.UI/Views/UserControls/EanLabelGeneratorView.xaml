<UserControl x:Class="ShelfMarket.UI.Views.UserControls.EanLabelGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:ShelfMarket.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:EanLabelGeneratorViewModel}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- Form -->
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <TextBlock Text="Shelf tenant" Margin="0,0,0,4"/>
                <ComboBox Width="200"
                          ItemsSource="{Binding ShelfTenants}"
                          SelectedItem="{Binding SelectedShelfTenant, Mode=TwoWay}"
                          IsEditable="False"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Shelf number" Margin="16,0,0,4"/>
                <ComboBox Width="140"
                          ItemsSource="{Binding ShelfNumbers}"
                          SelectedItem="{Binding SelectedShelfNumber, Mode=TwoWay}"
                          IsEditable="False"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Price" Margin="16,0,0,4"/>
                <TextBox Width="120"
                         HorizontalContentAlignment="Right"
                         Text="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>
            </StackPanel>

            <Button Content="Generate"
                    Margin="16,16,0,0"
                    Padding="14,6"
                    Command="{Binding GenerateCommand}"/>
        </StackPanel>

        <!-- Preview -->
        <Border Grid.Row="1" Margin="0,16,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Shelf:" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ShelfDisplay}"/>
                    <TextBlock Text="Price:" FontWeight="SemiBold" Margin="20,0,0,0"/>
                    <TextBlock Text="{Binding PriceDisplay}"/>
                    <TextBlock Text="EAN:" FontWeight="SemiBold" Margin="20,0,0,0"/>
                    <TextBlock Text="{Binding Ean}" FontFamily="Consolas"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <Image Source="{Binding BarcodeImage}" Stretch="None" />
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Errors -->
        <TextBlock Grid.Row="3"
                   Margin="0,12,0,0"
                   Foreground="Firebrick"
                   TextWrapping="Wrap"
                   Text="{Binding Error}"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>