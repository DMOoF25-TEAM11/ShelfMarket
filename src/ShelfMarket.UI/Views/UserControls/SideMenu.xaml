<UserControl x:Class="ShelfMarket.UI.Views.UserControls.SideMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShelfMarket.UI.Views.UserControls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Logo section -->
            <RowDefinition Height="Auto"/>
            <!-- Menu buttons section -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Height="Auto" Width="200">
            <StackPanel>
                <TextBlock>Kun lavet for at teste layout</TextBlock>
                <TextBlock>Martin vi mangler Logo her</TextBlock>
            </StackPanel>
        </Border>
        <Border  Grid.Row="1" Height="Auto" Width="200">

            <StackPanel Orientation="Vertical">
                <TextBlock>Kun lavet for at teste layout</TextBlock>
                <Button Content="Forside"/>
                <Button Content="Salg"/>
                <Button Content="Rapporter"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
