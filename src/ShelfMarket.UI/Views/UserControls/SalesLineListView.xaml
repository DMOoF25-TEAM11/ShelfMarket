<UserControl x:Class="ShelfMarket.UI.Views.UserControls.SalesLineListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShelfMarket.UI.Views.UserControls"
             mc:Ignorable="d" 
             xmlns:viewmodels="clr-namespace:ShelfMarket.UI.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:SalesLineListViewModel}">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Reoltype (liste)"/>
            <Border Grid.Row="1">
                <ListView ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  Grid.Row="0"
                  Margin="10">
                    <ListView.View>
                        <GridView>
                            <!--<GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}" />-->
                            <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Description" Width="400" DisplayMemberBinding="{Binding Description}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
            <TextBlock Grid.Row="2" Text="Automatisk opdatering ved gemning." />
        </Grid>
    </Grid>
</UserControl>
