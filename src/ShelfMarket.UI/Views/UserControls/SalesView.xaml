<UserControl x:Class="ShelfMarket.UI.Views.UserControls.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:uc="clr-namespace:ShelfMarket.UI.Views.UserControls"
             xmlns:controls="clr-namespace:ShelfMarket.UI.Views.UserControls" 
             xmlns:viewmodels="clr-namespace:ShelfMarket.UI.ViewModels" 
             xmlns:conv="clr-namespace:ShelfMarket.UI.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:SalesViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <!--
            Layout root:
            - 5 rows:
                0: Title (Auto)
                1: Form inputs (Auto)
                2: Action buttons (Auto)
                3: List/UserControl area (*)
                4: Status messages (Auto)
        -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Form Inputs: StackPanel with labeled TextBoxes for Name and Description -->
        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBlock Text="Reol nummer / EAN:"/>
            <TextBox x:Name="ReolTextBox"
                     Text="{Binding ShelfNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"
                     KeyDown="ReolTextBox_KeyDown"/>

            <TextBlock Text="Pris:"/>
            <TextBox x:Name="PriceTextBox"
                     Text="{Binding UnitPrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"
                     KeyDown="PriceTextBox_KeyDown"/>
        </StackPanel>

        <!-- Action Buttons: Horizontal StackPanel with Add, Save, Delete, and Cancel buttons -->
        <!--
          Buttons:
            - Visibility driven by IsAddMode/IsEditMode via BoolToVis converter.
            - Commands should be implemented in the ViewModel.
            - "Annuller" (Cancel) is always visible to reset or leave edit state.
        -->
        <!-- Action Buttons: Horizontal StackPanel with Add, Save, Delete, and Cancel buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0">
            <!-- New sale Button -->
            <Button Content="Nyt salg"
                    Command="{Binding BeginNewSaleCommand}"
                    Margin="5,0"/>
            <!-- Cancel Button (from CrudViewModel) -->
            <Button Content="Annuller"
                    Command="{Binding CancelCommand}"
                    Margin="5,0"/>
            <!-- Paid by Mobile -->
            <Button Content="Betalt med MobilePay"
                    Command="{Binding MobilePayCommand}"
                    Margin="5,0"/>
            <!-- Paid by Cash -->
            <Button Content="Betalt med Kontanter"
                    Command="{Binding CashPayCommand}"
                    Margin="5,0"/>
            <!-- Print Receipt -->
            <Button Content="Udskriv Kvittering"
                    Command="{Binding PrintReceiptCommand}"
                    Margin="5,0"/>
        </StackPanel>


        <ListView ItemsSource="{Binding SalesLines}" Grid.Row="3" Margin="0,10,0,10">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Background" Value="{x:Null}"/>
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Reol Number" DisplayMemberBinding="{Binding ShelfNumber}" />
                    <GridViewColumn Header="Pris">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:C2}, ConverterCulture=da-DK}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource Brush.TextDim}"/>
        </StackPanel>

    </Grid>
</UserControl>
