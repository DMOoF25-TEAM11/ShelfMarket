<UserControl x:Class="ShelfMarket.UI.Views.UserControls.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewmodels="clr-namespace:ShelfMarket.UI.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginRoleViewModel}"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <viewmodels:LoginRoleViewModel />
    </UserControl.DataContext>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
            <TextBlock Text="Rolle" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Width="160"
                      ItemsSource="{Binding Levels}"
                      SelectedItem="{Binding SelectedLevel, Mode=TwoWay}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
            <TextBlock Text="Adgangskode" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <PasswordBox x:Name="Pwd" Width="160" PasswordChanged="PasswordBox_OnPasswordChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Bekræft" Width="100" Margin="0,0,8,0" Command="{Binding ConfirmCommand}"/>
            <Button Content="Annuller" Width="100" Command="{Binding CancelCommand}"/>
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Style="{StaticResource Text.Error}"
                   Text="{Binding Error}"/>
    </Grid>
</UserControl>