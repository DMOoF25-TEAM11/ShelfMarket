<UserControl x:Class="ShelfMarket.UI.Views.Windows.FinalContractConfirmationWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Border Style="{StaticResource Popup.Window}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titel -->
            <TextBlock x:Name="TitleText" Text="Endelig kontrakt" Style="{StaticResource Text.H2}" HorizontalAlignment="Center"/>

            <!-- Knapper -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Tilføj kontrakt" Style="{StaticResource Button.Primary}" Height="38"/>
                <Button Grid.Column="2" Content="Annuller" Style="{StaticResource Button.Secondary}" Height="38"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
